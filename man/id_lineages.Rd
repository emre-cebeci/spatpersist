% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lineage_tables.R
\name{id_lineages}
\alias{id_lineages}
\title{Summarize persistent spatial identities and lineages}
\usage{
id_lineages(data, time)
}
\arguments{
\item{data}{A result returned by \code{\link[=persist_ids]{persist_ids()}}.}

\item{time}{A single character string naming the time column in \code{data}.}
}
\value{
A data frame with one row per persistent spatial identity.
}
\description{
Creates one row per \code{spatial_id} with its lineage, parent, observed time
span, number of observations and geometry versions, registry recovery count,
ambiguity count, and minimum non-missing match confidence.
}
\examples{
result <- persist_ids(example_units(), time = "year")
id_lineages(result, time = "year")

}
