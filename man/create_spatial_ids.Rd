% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_spatial_ids.R
\name{create_spatial_ids}
\alias{create_spatial_ids}
\title{Create persistent identifiers for longitudinal polygons}
\usage{
create_spatial_ids(
  data,
  time,
  threshold = 0.75,
  metric = c("share_old", "share_new", "iou"),
  match_rule = c("greedy", "mutual_best"),
  ambiguity_tolerance = 0.05,
  ambiguity_action = c("flag", "new", "error"),
  event_threshold = 0.1,
  lineage_threshold = 0.6,
  version_threshold = 1,
  geometry_action = c("error", "repair"),
  geometry_precision = NULL,
  max_time_gap = Inf,
  registry = NULL,
  registry_threshold = 0.999999
)
}
\arguments{
\item{data}{An \code{sf} object containing polygon geometries observed over time.}

\item{time}{A single character string naming the time column in \code{data}.}

\item{threshold}{A number from 0 to 1 giving the minimum overlap required
for continuity.}

\item{metric}{The overlap statistic used for matching: \code{"share_old"},
\code{"share_new"}, or \code{"iou"}.}

\item{match_rule}{Candidate selection rule: \code{"greedy"} considers every
eligible link, while \code{"mutual_best"} considers only links that are best
for both their predecessor and successor.}

\item{ambiguity_tolerance}{Maximum difference between eligible top scores
that is treated as a near tie.}

\item{ambiguity_action}{How near ties should be handled: \code{"flag"} selects
deterministically and marks the result, \code{"new"} rejects ambiguous links,
and \code{"error"} stops before assigning IDs.}

\item{event_threshold}{A number from 0 to 1 giving the minimum share of a
predecessor or successor used when detecting splits and mergers.}

\item{lineage_threshold}{A number from 0 to 1 giving the minimum share of
the smaller polygon that must overlap to connect two observations in the
same lineage. Accepted identity matches are always connected.}

\item{version_threshold}{A number from 0 to 1 giving the minimum IoU needed
for a continuing identity to retain its current geometry version. The
default of 1 creates a new version for any boundary change.}

\item{geometry_action}{How invalid polygon geometries should be handled:
\code{"error"} or \code{"repair"}. Repair uses \code{sf::st_make_valid()} and returns the
repaired geometries.}

\item{geometry_precision}{Optional positive precision scale passed to
\code{sf::st_set_precision()} before spatial operations. Coordinates are
rounded as \code{round(x * geometry_precision) / geometry_precision}; for
example, use 100 for two decimal places. \code{NULL} preserves input precision.}

\item{max_time_gap}{Maximum gap across which identities may continue.
Defaults to \code{Inf}. Finite values require a numeric, \code{Date}, or \code{POSIXt}
time column; \code{Date} gaps use days and \code{POSIXt} gaps use seconds.}

\item{registry}{Optional prior \code{sf} result from \code{create_spatial_ids()}.
Same-time geometries are used to preserve previously issued spatial,
geometry-version, and lineage identifiers across reruns.}

\item{registry_threshold}{Minimum same-time IoU required to recover an
observation from \code{registry}.}
}
\value{
The input \code{sf} object with identifier and transition columns,
including \code{spatial_id}, \code{spatial_version_id}, \code{lineage_id}, \code{parent_id},
\code{transition_type}, \code{match_score}, \code{match_confidence}, and
\code{match_ambiguous}. \code{registry_matched} indicates recovered observations.
}
\description{
Assigns persistent identifiers by comparing polygons in each pair of
successive observed time periods. A polygon continues an earlier identity
when its selected overlap statistic meets \code{threshold}. Candidate links are
selected one-to-one, so a persistent identifier cannot occur more than once
in the same time period.
}
\details{
Names and other non-spatial attributes are not used for matching. Unmatched
polygons receive new identifiers. Strong spatial links connect related
identities in a shared lineage and are used to describe splits, mergers,
replacements, and complex reorganizations.

Candidate-link diagnostics are stored with the result and can be retrieved
with \code{spatialid_transitions()}.
}
\examples{
data <- spatialid_example()
result <- create_spatial_ids(data, time = "year", threshold = 0.75)
result[, c("year", "name", "spatial_id")]
}
